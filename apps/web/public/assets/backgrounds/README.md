# Background assets

Add the final room art as `back-room.png` at 960 x 540 pixels. The path and
dimensions are declared in `src/assets/sceneAssets.ts`. Change its `ready` flag
to `true` when the file has been added.
