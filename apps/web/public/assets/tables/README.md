# Table assets

Add the transparent foreground table art as `blackjack-table.png` on a 960 x
540 transparent canvas so it layers precisely over the room. Its path is
declared in `src/assets/sceneAssets.ts`. Change its `ready` flag to `true` when
the file has been added.
