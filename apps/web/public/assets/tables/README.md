# Table assets

The current shared background already contains its table, so no separate table
file is loaded. The optional `blackjack-table.png` manifest entry remains for a
future 960 x 540 transparent overlay if the room and table are split later.
