export const DECORATION_SLOT = Object.freeze({
  name: "background-right",
  x: 820,
  y: 205
});

// Null equipment values mean use the built-in visual/audio defaults.
export const DEFAULT_ASSET_KEYS = Object.freeze({
  cardBack: "card-back-default",
  table: "table-default",
  music: "music-default",
  winSound: "win-sound-default",
  bustSound: "bust-sound-default"
});
