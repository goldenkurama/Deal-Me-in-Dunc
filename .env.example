# API
PORT=3000
CLIENT_ORIGIN=http://localhost:5173
APP_TIME_ZONE=America/New_York

# Local MySQL connection used by the API and migration script
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=fox_blackjack
DB_USER=fox_blackjack
DB_PASSWORD=replace-me

# Docker-only MySQL setup
MYSQL_ROOT_PASSWORD=replace-root-password

# Used by the later Caddy deployment example
GAME_DOMAIN=localhost
